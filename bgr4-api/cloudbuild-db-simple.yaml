steps:
  # Dockerコンテナのビルド
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/bgr4-api-db-simple:latest"
      - "-f"
      - "bgr4-api/Dockerfile.db-simple"
      - "."

  # GCRへのイメージのプッシュ
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/bgr4-api-db-simple:latest"

images:
  - "gcr.io/$PROJECT_ID/bgr4-api-db-simple:latest"

timeout: "1800s"
