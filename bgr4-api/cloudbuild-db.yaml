steps:
  # Dockerイメージのビルド
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/bgr4-api-db:v1"
      - "-f"
      - "Dockerfile.db"
      - "."

  # Google Container Registryへのプッシュ
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/bgr4-api-db:v1"

  # Cloud Runへのデプロイ
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      - "run"
      - "deploy"
      - "bgr4-api-db"
      - "--image=gcr.io/$PROJECT_ID/bgr4-api-db:v1"
      - "--region=asia-northeast1"
      - "--platform=managed"
      - "--allow-unauthenticated"
      - "--memory=512Mi"
      - "--cpu=1"
      - "--min-instances=0"
      - "--max-instances=2"
      - "--timeout=300s"
      - "--port=8080"
      - "--set-env-vars=RAILS_ENV=production,RAILS_LOG_TO_STDOUT=true,INITIALIZE_DB=true,DATABASE_HOST=$_DATABASE_HOST,DATABASE_PORT=$_DATABASE_PORT,DATABASE_USERNAME=$_DATABASE_USERNAME,DATABASE_PASSWORD=$_DATABASE_PASSWORD,DATABASE_NAME=$_DATABASE_NAME,SECRET_KEY_BASE=64b0847d9dcb93bdeb87994cbe3b060d4ddd5c2943936ab77aff0ff84ccd9fef91e60ec172f4deac2c7d85f4de1a68a1dbb0be1b0d0bdcd9f79ec75aea86efd7"

# 生成されるイメージの指定
images:
  - "gcr.io/$PROJECT_ID/bgr4-api-db:v1"

# タイムアウトの設定（30分）
timeout: "1800s"
