steps:
  # Dockerコンテナのビルド
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/bgr4-api-db-connect:latest"
      - "-f"
      - "bgr4-api/Dockerfile.db-connect"
      - "."

  # GCRへのイメージのプッシュ
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/bgr4-api-db-connect:latest"

images:
  - "gcr.io/$PROJECT_ID/bgr4-api-db-connect:latest"

timeout: "1800s"
