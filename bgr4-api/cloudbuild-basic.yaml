steps:
  # Dockerコンテナのビルド
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/bgr4-api-basic:latest"
      - "-f"
      - "bgr4-api/Dockerfile.basic"
      - "."

  # GCRへのイメージのプッシュ
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/bgr4-api-basic:latest"

images:
  - "gcr.io/$PROJECT_ID/bgr4-api-basic:latest"

timeout: "1800s"
